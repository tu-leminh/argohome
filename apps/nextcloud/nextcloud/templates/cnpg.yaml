apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nextcloud-db
spec:
  postgresUID: 1000
  postgresGID: 1000
  instances: 1
  storage:
    size: 5Gi
    storageClass: local-storage
  bootstrap:
    initdb:
      database: nextcloud
      owner: nextcloud