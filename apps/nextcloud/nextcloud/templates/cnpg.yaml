apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nextcloud-db
spec:
  instances: 1
  storage:
    size: 5Gi
    storageClass: local-storage
  bootstrap:
    initdb:
      database: nextcloud
      owner: nextcloud