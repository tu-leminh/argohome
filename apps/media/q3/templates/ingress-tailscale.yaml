apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: q3-tailscale
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
  - hosts:
    - q3
  defaultBackend:
    service:
      name: {{ include "common.names.fullname" . }}
      port:
        number: {{ .Values.q3.service.port }}
