apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: profilarr-tailscale
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
  - hosts:
    - profilarr
  defaultBackend:
    service:
      name: {{ include "common.names.fullname" . }}
      port:
        number: {{ .Values.profilarr.service.port }}
