apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.profilarr.service.type | default "ClusterIP" }}
  {{- if .Values.profilarr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.profilarr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.profilarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
