apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.bazarr.service.type | default "ClusterIP" }}
  {{- if .Values.bazarr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.bazarr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.bazarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
