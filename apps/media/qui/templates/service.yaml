apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.qui.service.type | default "ClusterIP" }}
  {{- if .Values.qui.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.qui.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.qui.service.port }}
      targetPort: webui
      protocol: TCP
      name: webui
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}