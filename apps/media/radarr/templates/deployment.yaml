apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "common.labels.selector" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "common.labels.selector" . | nindent 8 }}
    spec:
      # fsGroup is ignored on hostPath PVs, and containerd auto-creates the
      # shared data dir as root:root - chmod the top-level dirs (not -R,
      # library is huge) to 777 so any app, regardless of uid, can
      # create/move files under them.
      initContainers:
        - name: fix-perms
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              chmod 777 /data /data/movies /data/downloads 2>/dev/null || true
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            {{- if .Values.radarr.persistence.data.enabled }}
            - name: data
              mountPath: /data
            {{- end }}
      containers:
        - name: radarr
          image: "{{ .Values.radarr.image.repository }}:{{ .Values.radarr.image.tag }}"
          imagePullPolicy: {{ .Values.radarr.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.radarr.service.containerPort }}
              protocol: TCP

          env:
            {{- range $key, $value := .Values.radarr.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}

          volumeMounts:
            {{- if .Values.radarr.persistence.config.enabled }}
            - name: config
              mountPath: /config
            {{- end }}
            {{- if .Values.radarr.persistence.data.enabled }}
            - name: data
              mountPath: {{ .Values.radarr.persistence.data.mountPath }}
            {{- end }}

      volumes:
        {{- if .Values.radarr.persistence.config.enabled }}
        - name: config
          persistentVolumeClaim:
            claimName: {{ .Values.radarr.persistence.config.existingClaim }}
        {{- end }}
        {{- if .Values.radarr.persistence.data.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.radarr.persistence.data.existingClaim }}
        {{- end }}
