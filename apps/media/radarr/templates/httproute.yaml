{{- range $name, $target := .Values.radarr.ingressTargets }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radarr-{{ $name }}
  namespace: {{ $.Release.Namespace }}
spec:
  parentRefs:
    - name: infra-gateway
      namespace: infra
      group: gateway.networking.k8s.io
      kind: Gateway
  hostnames:
    - "{{ $target.host }}"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "common.names.fullname" $ }}
          port: {{ $.Values.radarr.service.port }}
          group: ""
          kind: Service
          weight: 1
{{- end }}
