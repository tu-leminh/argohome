apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.qbittorrent.service.type | default "ClusterIP" }}
  {{- if .Values.qbittorrent.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.qbittorrent.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.qbittorrent.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 6881
      targetPort: bittorrent
      protocol: TCP
      name: bittorrent
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
