apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: q1-tailscale
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
  - hosts:
    - q1
  defaultBackend:
    service:
      name: {{ include "common.names.fullname" . }}
      port:
        number: {{ .Values.q1.service.port }}
