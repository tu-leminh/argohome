apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.seerr.service.type | default "ClusterIP" }}
  {{- if .Values.seerr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.seerr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.seerr.service.port }}
      targetPort: webui
      protocol: TCP
      name: webui
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}