{{- range $name, $target := .Values.deluge.ingressTargets }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: deluge-{{ $name }}
  namespace: {{ $.Release.Namespace }}
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`{{ $target.host }}`){{- if $target.path }} && PathPrefix(`{{ $target.path }}`){{- end }}
    kind: Rule
    services:
    - name: {{ include "common.names.fullname" $ }}
      port: {{ $.Values.deluge.service.port }}
  tls:
    certResolver: {{ $target.certResolver }}
    {{- if $target.tlsDomain }}
    domains:
    - main: "{{ $target.tlsDomain }}"
      {{- if $target.tlsSans }}
      sans:
        {{- range $target.tlsSans }}
        - "{{ . }}"
        {{- end }}
      {{- end }}
    {{- end }}
{{- end }}
