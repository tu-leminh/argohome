apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deluge-tailscale
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
  - hosts:
    - deluge
  defaultBackend:
    service:
      name: {{ include "common.names.fullname" . }}
      port:
        number: {{ .Values.deluge.service.port }}
