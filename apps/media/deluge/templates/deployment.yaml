apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "common.labels.selector" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "common.labels.selector" . | nindent 8 }}
    spec:
      containers:
        - name: deluge
          image: "{{ .Values.deluge.image.repository }}:{{ .Values.deluge.image.tag }}"
          imagePullPolicy: {{ .Values.deluge.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.deluge.service.port }}
              protocol: TCP
            - name: daemon
              containerPort: 58846
              protocol: TCP
            - name: incoming-tcp
              containerPort: 6881
              protocol: TCP
            - name: incoming-udp
              containerPort: 6881
              protocol: UDP
          
          env:
            {{- range $key, $value := .Values.deluge.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          
          volumeMounts:
            {{- if .Values.deluge.persistence.config.enabled }}
            - name: config
              mountPath: /config
            {{- end }}
            {{- if .Values.deluge.persistence.downloads.enabled }}
            - name: downloads
              mountPath: {{ .Values.deluge.persistence.downloads.mountPath }}
            {{- end }}
      
      volumes:
        {{- if .Values.deluge.persistence.config.enabled }}
        - name: config
          persistentVolumeClaim:
            claimName: {{ .Values.deluge.persistence.config.existingClaim }}
        {{- end }}
        {{- if .Values.deluge.persistence.downloads.enabled }}
        - name: downloads
          persistentVolumeClaim:
            claimName: {{ .Values.deluge.persistence.downloads.existingClaim }}
        {{- end }}
