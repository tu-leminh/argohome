apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.uploadAssistant.service.type | default "ClusterIP" }}
  {{- if .Values.uploadAssistant.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.uploadAssistant.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.uploadAssistant.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
