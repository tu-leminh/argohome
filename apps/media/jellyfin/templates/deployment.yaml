apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "common.labels.selector" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "common.labels.selector" . | nindent 8 }}
    spec:
      # fsGroup is ignored on hostPath PVs, and containerd auto-creates the
      # shared data dir as root:root - chmod the top-level dirs (not -R,
      # library is huge) to 777 so any app, regardless of uid, can access.
      initContainers:
        - name: fix-perms
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              chmod 777 /data /data/shared /data/shared/movies /data/shared/shows /data/shared/music 2>/dev/null || true
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            {{- if .Values.jellyfin.persistence.data.enabled }}
            - name: data
              mountPath: /data
            {{- end }}
      containers:
        - name: jellyfin
          image: "{{ .Values.jellyfin.image.repository }}:{{ .Values.jellyfin.image.tag }}"
          imagePullPolicy: {{ .Values.jellyfin.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.jellyfin.service.port }}
              protocol: TCP

          env:
            {{- range $key, $value := .Values.jellyfin.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}

          volumeMounts:
            {{- if .Values.jellyfin.persistence.config.enabled }}
            - name: config
              mountPath: /config
            {{- end }}
            {{- if .Values.jellyfin.persistence.data.enabled }}
            - name: data
              mountPath: {{ .Values.jellyfin.persistence.data.mountPath }}
            {{- end }}

      volumes:
        {{- if .Values.jellyfin.persistence.config.enabled }}
        - name: config
          persistentVolumeClaim:
            claimName: {{ .Values.jellyfin.persistence.config.existingClaim }}
        {{- end }}
        {{- if .Values.jellyfin.persistence.data.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.jellyfin.persistence.data.existingClaim }}
        {{- end }}
