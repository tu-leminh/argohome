apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rtorrent-tailscale
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
  - hosts:
    - rtorrent
  defaultBackend:
    service:
      name: {{ include "common.names.fullname" . }}
      port:
        number: {{ .Values.rtorrent.service.port }}
