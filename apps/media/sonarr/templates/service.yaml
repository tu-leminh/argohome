apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.sonarr.service.type | default "ClusterIP" }}
  {{- if .Values.sonarr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.sonarr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.sonarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
