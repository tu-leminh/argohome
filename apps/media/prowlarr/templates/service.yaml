apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.prowlarr.service.type | default "ClusterIP" }}
  {{- if .Values.prowlarr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.prowlarr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.prowlarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
