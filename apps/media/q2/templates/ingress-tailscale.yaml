apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: q2-tailscale
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  tls:
  - hosts:
    - q2
  defaultBackend:
    service:
      name: {{ include "common.names.fullname" . }}
      port:
        number: {{ .Values.q2.service.port }}
