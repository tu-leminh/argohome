apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.lidarr.service.type | default "ClusterIP" }}
  {{- if .Values.lidarr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.lidarr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.lidarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
