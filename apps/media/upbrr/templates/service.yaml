apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.upbrr.service.type | default "ClusterIP" }}
  {{- if .Values.upbrr.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.upbrr.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.upbrr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
