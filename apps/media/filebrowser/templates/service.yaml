apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
  type: {{ .Values.filebrowser.service.type | default "ClusterIP" }}
  {{- if .Values.filebrowser.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.filebrowser.service.loadBalancerIP }}
  {{- end }}
  ports:
    - port: {{ .Values.filebrowser.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "common.labels.selector" . | nindent 4 }}
