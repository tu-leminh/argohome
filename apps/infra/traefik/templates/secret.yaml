# ⚠️ RUN: echo -n "YOUR_TOKEN" | base64
apiVersion: v1
kind: Secret
metadata:
  name: dns-tokens
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  DUCKDNS_TOKEN: "PASTE_BASE64_HERE"
  FREEMYIP_TOKEN: "PASTE_BASE64_HERE"
